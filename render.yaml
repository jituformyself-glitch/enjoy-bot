services:
  - type: web
    name: enjoy-bot2
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python enjoybot.py"
    envVars:
      - key: PORT
        value: "10000"
      - key: BOT_TOKEN
        sync: false
      - key: RENDER_EXTERNAL_URL
        sync: false
